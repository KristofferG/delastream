
<div class="mt-5" align="center">

    <%= image_tag("delastream.png", :alt => "De La Stream", :width => "260", :class => "mb-5") %><br>

    <div class="col-xs-12 col-md-8 col-xl-6">
      <div class="bg-dark user-wrapper">
        <div class="m-3">
          <%= form_with(url: login_path, scope: :session, local: true) do |form| %>

            <div class="danger">
              <%= flash[:danger] %>
            </div>

            <div class="form-group" align="left">
              <p>
                <%= form.label :epost, :for => 'email' %>
                <%= form.email_field :email, :class => 'form-control', :id =>'email' %>
              </p>
              <p>
                <%= form.label :lösenord, :for => 'password' %>
                <%= form.password_field :password, :class => 'form-control', :id =>'password' %>
              </p>

              <%= form.submit "Logga in",  :class => 'btn btn-primary btn-block mt-4' %>
              <% end %>
            </div>
            <div class="mt-3 body-text" align="center">
              <%= link_to 'Skapa konto', new_user_path %>
            </div>
          </div>
        </div>
        <div class="mt-3 small-text dark-text" align="center">
          &copy; Maxkungen 2020
        </div>
    </div>
  </div>
